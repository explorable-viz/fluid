letrec
   fun zipW op → fun {
      Nil → fun ys → Nil;
      Cons(x, xs) → fun {
         Nil → Nil;
         Cons(y, ys) → Cons(op(x, y), zipW op xs ys)
      }
   }
in
zipW
   (fun (x, y) → Point(x, y))
   Cons(10, Cons(12, Cons(13, Cons(20, Nil))))
   Cons(3, Cons(4, Cons(8, Cons(16, Nil))))
