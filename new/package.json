{
  "scripts": {
    "start": "spago bundle-app --main Test.Main --to dist/app.js && parcel serve index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "purescript": "^0.13.6",
    "spago": "^0.15.2"
  }
}
