{
  "license": "MIT",
  "scripts": {
    "build": "spago bundle-app --main Test.Main --to dist/app.js && parcel build index.html",
    "start": "spago bundle-app --main Test.Main --to dist/app.js && parcel serve index.html",
    "test": "spago test"
  },
  "staticFiles": {
    "staticPath": {
      "staticPath": "fluid",
      "staticOutDir": "fluid"
    },
    "watcherGlob": "**"
  },
  "devDependencies": {
    "mocha": "^7.1.2",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.3.1",
    "purescript": "^0.13.6",
    "spago": "^0.15.2"
  },
  "dependencies": {
    "karma": "^5.0.8",
    "karma-chrome-launcher": "^3.1.0",
    "karma-mocha-reporter": "^2.2.5",
    "karma-spec-reporter": "^0.0.32",
    "xhr2": "^0.2.0"
  }
}
