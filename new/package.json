{
  "scripts": {
    "start": "spago bundle-app --main Test.Main --to dist/app.js && parcel serve index.html",
    "test": "spago bundle-app --main Test.Main --to dist/app.js && parcel serve index.html && spago test"
  },
  "staticFiles": {
    "staticPath": {
      "staticPath": "fluid",
      "staticOutDir": "fluid"
    },
    "watcherGlob": "**"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.3.1",
    "purescript": "^0.13.6",
    "spago": "^0.15.2"
  },
  "dependencies": {
    "xhr2": "^0.2.0"
  }
}
