name: develop

on: [workflow_call]

jobs: 
  build-and-test:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
          node-version: 22
    - name: build-and-test
      run: |
        yarn install
        . script/git/hooks/pre-push