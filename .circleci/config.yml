#
# Build configuration for Circle CI
#

# machine:
#  node:
#    version: 7.4.0

# dependencies:
#  override:
#    - npm install

# test:
#    override:
#        - npm run ci-test
#        # copy test results
#        - mkdir $CIRCLE_TEST_REPORTS/junit
#        - mv node_modules/junit-test-results/* $CIRCLE_TEST_REPORTS/junit

# checkout:
#  post:
#    - git submodule sync
#    - git submodule update --init

# general:
#  branches:
#    only:
#      - master

version: 2 # use CircleCI 2.0
jobs: # a collection of steps
  build: # runs not using Workflows must have a `build` job as entry point
    working_directory: ~/mern-starter # directory where steps will run
    docker: # must specify one of "macos", "machine", "docker"; not sure what the numbers mean though:
      - image: circleci/node:7.4.0 # ...with this image as the primary container; this is where all `steps` will run
      - image: mongo:3.4.4 # and this image as the secondary service container
    steps: # a collection of executable commands 
      - checkout # special step to check out source code to working directory
      - run:
          name: update-npm 
          command: 'sudo npm install -g npm@latest'
      - restore_cache: # special step to restore the dependency cache 
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: install-npm-wee
          command: npm install
      - save_cache: # special step to save the dependency cache
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules
      - run: # run tests
          name: test
          command: npm test
        