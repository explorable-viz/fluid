def zip_with(op, [], ys): []
def zip_with(op, x :| xs, []): []
def zip_with(op, x :| xs, y :| ys): op(x, y) :| zip_with(op, xs, ys)

zip_with(lambda x, y: x ** ⸨2⸩ + y ** ⸨2⸩, [2, ⸨3⸩, 4], [3, ⸨4⸩, 5, 6])
