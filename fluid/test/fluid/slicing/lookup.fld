import slicing.lookup_inputs

def lookup(k, Empty): None
def lookup(k, NonEmpty(t1, (key, val), t2)):
  match compare(k, key):
    case LT: lookup(k, t1)
    case EQ: Some(val)
    case GT: lookup(k, t2)

lookup(k, tree)
