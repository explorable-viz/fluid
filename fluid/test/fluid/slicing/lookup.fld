import slicing.lookup_inputs

def lookup(k, Empty):
  return Nothing
def lookup(k, NonEmpty(t1, (key, val), t2)):
  match compare(k, key):
    case LT:
      return lookup(k, t1)
    case EQ:
      return Just(val)
    case GT:
      return lookup(k, t2)

lookup(k, tree)
