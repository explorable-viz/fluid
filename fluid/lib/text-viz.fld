let computeProb (min, max) target =
    if target > max
    then 0.0
    else if target < min
    then 1.0
    else (target - min)/(max - min);

let likelihoodMap table prob = match find (fun x -> x.prob <= prob) table as {
    None -> error "absurd";
    Some x -> x.msg ++ ""
};