let chinaData = [ row | row <- data, row.country == "China" ];
let series type rows = [ {x: year, y: row.output } |
       year <- [2013..2015], row <- rows,
       row.year == year, row.type == type
    ]
in LineChart {
   caption: "Growth in renewables for China",
   data: chinaData
}
