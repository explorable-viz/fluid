let data2015 = [ row | row <- data, row.year == 2015 ];
let total c = sum [ row.output | row <- data2015, row.country == c ];
let countryData = [ { x: c, y: total c } | c <- ["China", "USA", "Germany"] ]
in BarChart countryData
