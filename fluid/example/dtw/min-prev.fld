let minAndPrev (i, j) im1 jm1 ijm1 = 
    let minimal = minimum [im1,jm1,ijm1]
    in 
    if minimal == im1 
    then (((i - 1), j), minimal)
    else if minimal == jm1
    then ((i, (j - 1)), minimal)
    else
        (((i - 1), (j - 1)), ijm1)
in minAndPrev (4,5) 3 4 5