let abs x y = if x - y < 0 then negate (x - y) else (x - y);
    costMatrixInit rows cols window = 
      let ns = [0 .. rows];
          ms = [0 .. cols]
      in 
        [| let initV = if (n == 0 `and` m == 0) `or` ((abs (n - m) <= window) `and` (not (n == 0 `or` m == 0))) 
                       then FNum 0
                       else Infty 
           in initV | (n, m) in (rows, cols) |]
in costMatrixInit 5 7 1
     
