let usa =
   [ row | row <- data, row.country == "USA" ];
let series rows = [
   { x: year, y: row.output }
   | year <- [2015], row <- rows,
     row.year == year, row.energyType == "Bio"
] in series usa
