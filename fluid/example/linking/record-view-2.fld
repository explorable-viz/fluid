let usa =
   [ row | row <- data, row.country == "USA" ];
let series type rows = [
   { x: year, y: row.output }
   | year <- [2015], row <- rows,
     row.year == year, row.energyType == type
] in LinePlot { name: "Bio", data: series "Bio" usa }
