let chartForYear = 
   fun year → 
      let bars = flip map (lookup year data) (compose barChart snd) in 
      Graphic(0, 0, length bars, max (map height bars), spaceRight 1 bars, None)
in map (compose chartForYear fst) data
