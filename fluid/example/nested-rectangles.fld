// List (a, List (b, Num)) → GraphicsElement
let chart = fun data → 
   stackRight (flip map data (compose (scaleToWidth 1) (compose barChart snd)))
in stackRight (map (compose (scaleToWidth 1) (compose chart snd)) data)
