let timeSeries = 
   flip map data 
      (fun (year, yearData) → (year, lookup "Bio" (lookup "China" yearData)));
let start = min (map fst timeSeries) 
in
Polyline(None, flip map timeSeries (fun (year, n) → (year - start, n)))
