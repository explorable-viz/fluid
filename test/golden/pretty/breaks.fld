let zero = 0;

let one = 1;

let int True = one;
    int False = zero;

let float True = 1.0;
    float False = 0.0;


let f x = let g x y = x + y in let h c t f = if c then t else f in let y = zero in let z = zero in g y z

in 0
