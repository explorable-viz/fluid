import figure1_data

def chapter:
  dict([ (risk[ "ID#" ], risk[ "Chapter" ]) for risk in risks ])

def split_data:
  groupBy(lambda r1, r2: chapter[ r1 [ "Risk ID#" ] ] == chapter [ r2 [ "Risk ID#" ] ], eval_data)

"""Grouped by ${length(split_data)} chapters."""
