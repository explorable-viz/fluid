def lookup(k, Empty):
  return None
def lookup(k, NonEmpty(left, (key, val), right)):
  match compare(k, key):
    case LT:
      return lookup(k, left)
    case EQ:
      return Some(val)
    case GT:
      return lookup(k, right)
