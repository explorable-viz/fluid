{
  "name": "@fluid-org/monorepo",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "fluid",
    "website/*"
  ],
  "scripts": {
    "prepare": "git rev-parse --is-inside-work-tree >/dev/null 2>&1 && git config core.hooksPath script/git/hooks || true",
    "test-website": "./script/test-website.sh",
    "test-website-all": "./script/test-website-all.sh"
  }
}
